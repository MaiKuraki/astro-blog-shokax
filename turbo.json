{
  "$schema": "https://turborepo.com/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["check", "lint", "test"],
      "outputs": ["dist/**"]
    },
    "check": {
      "outputs": ["src/**"]
    },
    "lint": {
      "outputs": ["src/**"]
    },
    "test": {
    },
    "dev": {
      "persistent": true,
      "cache": false
    }
  }
}
